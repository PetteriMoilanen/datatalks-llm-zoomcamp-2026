{
  "name": "Python + uv",
  "image": "mcr.microsoft.com/devcontainers/python:3.14",
  "postCreateCommand": "python -m pip install --upgrade pip && python -m pip install --user pipx && python -m pipx ensurepath || true && python -m pipx install uv || python -m pip install uv",
  "features": {},
  "customizations": {
    "vscode": {}
  }
}
